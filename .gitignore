# =======================================================
# 1. [보안 0순위] 절대 공개 금지 (Secrets)
# =======================================================
# 환경변수 파일
.env
.env.*
env.py

# 인증서 및 키
*.pem
*.key
*.crt
id_rsa
id_rsa.pub

# 주요 서비스 비밀 파일
secrets.json
client_secret.json
credentials.json
token.json
key.txt
config_private.toml
.streamlit/secrets.toml

# =======================================================
# 2. [Python] 실행 시 자동 생성되는 쓰레기들
# =======================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
.ipynb_checkpoints/
*.pyc

# =======================================================
# 3. [가상환경] 내 컴퓨터 전용 라이브러리 방
# =======================================================
.venv/
venv/
env/
ENV/
.Python

# =======================================================
# 4. [데이터] Git은 코드가 아닌 데이터를 저장하지 않음
# =======================================================
# 주의: 설정 파일(*.json, *.xml)까지 막지 않도록 확장자 주의
*.csv
*.tsv
*.xlsx
*.xls
*.parquet
*.avro
*.db
*.sqlite3
*.h5
*.hdf5

# 데이터 폴더 통째로 막기 (추천)
data/
outputs/
downloads/
raw_data/
processed_data/

# =======================================================
# 5. [모델 & 피클] 용량 주범 & 보안 위험 (Model)
# =======================================================
*.pickle
*.pkl
*.pckl
*.joblib
*.pt
*.pth
*.model
*.ckpt

# =======================================================
# 6. [압축 파일] 바이너리 파일 차단 (Archives)
# =======================================================
*.zip
*.tar
*.tar.gz
*.tgz
*.rar
*.7z
*.gz

# =======================================================
# 7. [로그] 실행 기록 (Logs)
# =======================================================
*.log
npm-debug.log*
yarn-debug.log*
celerybeat-schedule

# =======================================================
# 8. [빌드/배포] 임시 파일 (Builds)
# =======================================================
dist/
build/
*.egg-info/
.eggs/

# =======================================================
# 9. [OS/IDE] 운영체제 및 에디터 설정 (System)
# =======================================================
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.iml

# =======================================================
# 10. [Java/Spark] 스파크 사용 시 발생 (JVM)
# =======================================================
*.class
*.jar
*.war
target/

# =======================================================
# 11. [Airflow] Airflow 실행 부산물 및 설정 
# =======================================================
# 실행 로그 및 심볼릭 링크
logs/
latest

# 로컬 DB 및 설정 (보안상 절대 올리면 안 됨)
airflow.db
airflow.cfg
webserver_config.py
standalone_admin_password.txt
airflow-webserver.pid

# Docker 마운트용 빈 폴더 (필요시 주석 해제하여 사용)
plugins/
config/

# =======================================================
# ✅ [필수 허용] 예외 규칙 (Whitelist)
# =======================================================
# 락 파일은 팀원과 버전 동기화를 위해 "반드시" 올려야 함!
!uv.lock
!poetry.lock
!package-lock.json
!yarn.lock

# 프로젝트 설정 파일도 필수
!pyproject.toml